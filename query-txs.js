const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient({ datasources: { db: { url: 'postgresql://postgres:OlHmlmhhCKAMLVeFkbCwpvAkJbfafund@ballast.proxy.rlwy.net:49188/railway' } } }); prisma.walletTransaction.findMany({ where: { type: 'PIX' }, orderBy: { createdAt: 'desc' }, take: 5, include: { wallet: true } }).then(t => console.dir(t, { depth: null })).finally(() => prisma.$disconnect());
