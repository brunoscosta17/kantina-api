{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && echo 'DATABASE_URL available:' && echo $DATABASE_URL && echo 'Starting Prisma migrations...' && pnpm exec prisma migrate deploy --schema=src/prisma/schema.prisma && echo 'Migrations completed!' && pnpm exec prisma generate --schema=src/prisma/schema.prisma && pnpm run build"
  },
  "deploy": {
    "startCommand": "node dist/main",
    "sleepApplication": false,
    "restartPolicyType": "ALWAYS"
  }
}
