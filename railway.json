{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pnpm install && echo 'Starting Prisma migrations...' && pnpm exec prisma migrate deploy --schema=src/prisma/schema.prisma && echo 'Migrations completed!' && pnpm run build"
  },
  "deploy": {
    "startCommand": "pnpm run start:prod:migrate",
    "sleepApplication": false,
    "restartPolicyType": "ALWAYS"
  }
}
